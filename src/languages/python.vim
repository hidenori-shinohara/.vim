" Use flake8 as a python checker.
" flake8 seems less strict than pylint.
" Make sure to use https://pypi.org/project/pep8-naming/
" to catch naming issues such as the use of camel cases.
" let g:syntastic_python_checkers = ['flake8']
